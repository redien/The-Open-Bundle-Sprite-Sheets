
// Start with naive implementation that tries to balance
// columns/rows.

define(function () {
    var ImagePacker = function () {
        this.columns = 1;
        this.rows = 1;

        this.x = 0;
        this.y = 0;
    };

    ImagePacker.prototype.add = function (image) {

    };

    ImagePacker.prototype.write = function () {
        
    };
    
    return ImagePacker;
});
